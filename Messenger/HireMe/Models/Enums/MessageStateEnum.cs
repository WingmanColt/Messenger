namespace HireMe.Models.Enums
{
    public enum MessageStates
    {
        Read,
        Stared,
        Important,
    }

    public enum MessageClient
    {
        Sender,
        Receiver
    }
}